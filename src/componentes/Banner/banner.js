import './banner.css'

function Banner() {
    // JSX PARECE HTML MAS NÃO É ELE É JSX
    // Return de multiplas linhas
    return (
        <header className='banner'>
            <img src="/imagens/banner.png" alt="Banner"></img>
        </header>
    )
}

export default Banner
// Precisamos exportar o function